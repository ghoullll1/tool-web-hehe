<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.toolweb.platform.tool.fileshare.dao.TemporaryFileShareMapper">

    <sql id="temporaryFileShareColumns">
        id, share_id, access_key_digest, object_key, object_path, original_filename, content_type,
        size_bytes, sha256, expires_at, download_count, max_downloads, status,
        created_at, consumed_at, deleted, deleted_at, version
    </sql>

    <select id="selectForUpdate" resultType="com.toolweb.platform.tool.fileshare.entity.TemporaryFileShare">
        SELECT <include refid="temporaryFileShareColumns"/>
        FROM temporary_file_share
        WHERE share_id = #{shareId}
          AND deleted = FALSE
        FOR UPDATE
    </select>

    <select id="selectExpiredActive" resultType="com.toolweb.platform.tool.fileshare.entity.TemporaryFileShare">
        SELECT <include refid="temporaryFileShareColumns"/>
        FROM temporary_file_share
        WHERE deleted = FALSE
          AND expires_at &lt;= #{expiresAt}
        ORDER BY expires_at
        LIMIT #{limit}
    </select>

    <select id="existsActiveByObjectKey" resultType="boolean">
        SELECT EXISTS(
            SELECT 1
            FROM temporary_file_share
            WHERE object_key = #{objectKey}
              AND deleted = FALSE
        )
    </select>

    <update id="markDeleted">
        UPDATE temporary_file_share
        SET deleted = TRUE,
            deleted_at = #{deletedAt},
            version = version + 1
        WHERE share_id = #{shareId}
          AND deleted = FALSE
          AND version = #{version}
    </update>

</mapper>
